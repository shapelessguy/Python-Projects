<?xml version="1.0" encoding="utf-8"?>
<!-- DEV ONLY: the API is plain HTTP on the LAN / emulator host. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
